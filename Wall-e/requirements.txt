# Core (required)
ollama==0.6.2         # local model server client; drives the Ollama LLM
rich==15.0.0          # terminal UI (tui.py — kept for the original terminal mode)

# Web framework
Django==6.1.1          # the web framework powering the ChatGPT-like UI
djangorestframework==3.18.1  # REST API endpoints
django-crispy-forms==2.7      # form rendering helper
crispy-bootstrap5==2026.9     # Bootstrap 5 theme for crispy-forms
django-htmx==1.29.0           # HTMX support for progressive enhancement

# TTS: edge (online neural voices, default engine)
edge-tts==7.2.8       # Microsoft neural voices via the edge-tts CLI; needs internet

# TTS: kokoro (offline neural voices, optional) -- uncomment to use
# kokoro-onnx==0.6.1  # offline neural TTS; needs the model files in ./models
# soundfile           # writes kokoro output to .wav for playback
